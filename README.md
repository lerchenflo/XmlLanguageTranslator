This is a Kotlin Multiplatform project targeting Desktop (JVM).

# How to use
## Run desktop (Android Studio)
* Add new gradle build
* Run inputfield: runDesktop
* run it!

## Usage
* Load the default file first (This order will be kept). Comments etc will also be loaded and not deleted.
* Load the other files
* Top right corner: Sort for not translated strings